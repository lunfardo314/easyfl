# EasyFL — TinyGo / WASM Compatibility

**This document has moved.** The canonical audit + execution plan now
lives in the Proxima repo, together with its proxima-side companion,
because Proxima is the consumer driving the wasm refactor:

- `proxima/claude/wasm_easyfl.md` — the easyfl-side audit + Phase A–D
  implementation plan (the full content that used to be in this file).
- `proxima/claude/wasm_txbuilder.md` — the proxima-side `txcore`
  design + Phase 0–7 plan that depends on the easyfl work landing.

Moved on 2026-05-18. The split into two docs under one repo keeps the
multi-repo refactor in a single source of truth without mashing two
different audiences (easyfl internals vs proxima wallet API) into one
mega-document.
